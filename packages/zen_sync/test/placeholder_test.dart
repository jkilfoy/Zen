import 'package:test/test.dart';

void main() {
  test('M0: the zen_sync package resolves, analyzes and runs its tests', () {
    // A placeholder so the suite is non-empty (§11.13, M0). The path
    // dependencies themselves are proven by `pub get` and `dart analyze`.
    // Replaced by real tests at this package's milestone.
    expect(1, 1);
  });
}
