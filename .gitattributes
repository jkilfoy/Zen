# Normalise line endings so `dart format --set-exit-if-changed` agrees between
# a Windows working copy and the Linux CI runner (§11.10).
* text=auto eol=lf
*.bat text eol=crlf
*.ps1 text eol=crlf
*.png binary
*.jpg binary
*.ico binary
*.keystore binary
*.jks binary
